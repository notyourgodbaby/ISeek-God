# ISeekGod Environment Variables (Optional for local development)
# No API keys or credentials required - all analysis runs locally

# Python settings (if using)
PYTHONPATH=.

# Go settings (if using)
# None required

# Rust settings (if using)
# None required

# TypeScript settings (if using)
# None required
